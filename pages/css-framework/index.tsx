'use client';
import React from 'react';
import Container from '../../components/Container';
import { css } from '../../data/cssFramework';

const CSS = () => {
  return (
    <Container
      tools={css}
      title="CSS Framework"
    />
  );
};

export default CSS;

'use client';
import React from 'react';
import Container from '../../components/Container';
import { Icons } from '../../data/icons';

const Icon = () => {
  return (
    <Container tools={Icons} title="Icons" />
  );
};

export default Icon;
